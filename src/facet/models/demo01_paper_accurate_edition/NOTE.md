# demo01 — no paper-accurate edition applicable

`demo01` is a **frozen internal proof-of-concept** (the first seven-epoch
context CNN), not based on a publication (see
[`../REFERENCES.md`](../REFERENCES.md), *Internal prototypes* table: *"Frozen
proof-of-concept (7-epoch context CNN); not based on a publication."* — and
`../README.md`, "Demo 01 Status").

Because there is **no external source paper** to compare against, a
"paper-accurate edition" is undefined for this model, and `demo01` is
explicitly frozen as a demo. The existing [`../demo01/`](../demo01/) directory
is left untouched.
