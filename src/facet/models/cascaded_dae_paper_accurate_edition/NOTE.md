# cascaded_dae — no paper-accurate edition applicable

`cascaded_dae` is an **internal FACETpy prototype**, not an adaptation of a
published architecture (see [`../REFERENCES.md`](../REFERENCES.md), *Internal
prototypes* table: *"Channel-wise cascaded denoising autoencoder; ported from
the older FACETpy `feature/deeplearning` prototype."*).

Because there is **no external source paper** to compare against, a
"paper-accurate edition" is undefined for this model. The existing
[`../cascaded_dae/`](../cascaded_dae/) directory already *is* the reference
implementation — there is nothing to make more faithful to.

If a published paper is later adopted as this model's basis, replace this note
with a real `_paper_accurate_edition` derived from that paper, following the
same workflow used for the paper-backed models.
